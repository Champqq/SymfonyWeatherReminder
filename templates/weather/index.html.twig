{% extends 'base.html.twig' %}

{% block title %}Прогноз погоди{% endblock %}

{% block body %}
    <h1>Прогноз погоди для {{ city }}</h1>

    {% if forecast %}
        <ul>
            {% for day in forecast %}
                <div style="margin-bottom: 15px;">
                    <strong>{{ day.date|date('l, d M Y') }}</strong><br>
                    {{ day.description|capitalize }}<br>
                    Температура: {{ day.temperature }}°C
                </div>
            {% endfor %}
        </ul>
    {% else %}
        <p>Немає даних про погоду.</p>
    {% endif %}
{% endblock %}
